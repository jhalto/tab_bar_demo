import 'package:flutter/cupertino.dart';

class CallsDemo extends StatelessWidget {
  const CallsDemo({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Container(
        child: Text("Calls"),
      ),
    );
  }
}
